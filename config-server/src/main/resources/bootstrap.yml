# TODO: How to filter like application.yml?
#info:
#  app:
#    name: @project.name@
#    description: @project.description@
#    version: @project.version@
#  build:
#    artifact: @project.artifactId@
#    name: @project.name@
#    description: @project.description@
#    version: @project.version@

server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
