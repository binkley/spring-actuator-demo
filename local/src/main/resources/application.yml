info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@
  build:
    artifact: @project.artifactId@
    name: @project.name@
    description: @project.description@
    version: @project.version@
  j-random:
    extra-data: Frodo lives!

spring:
  datasource:
    url: jdbc:derby:memory:helloWorld;create=true
